using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class InGame4UI_UncleTime : UIComponent
{
    //Text만 가지고 있다가 Uncle이 터치 시 드러남
    /*Text uncleTimeText;

    public void SetUncleTimer(string timerInfo)
    {
        uncleTimeText.text = timerInfo;
    }

    private void Awake()
    {
        uncleTimeText = this.GetComponent<Text>();
    }
    */
}
